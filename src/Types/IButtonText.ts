export interface IButtonText {
  layout: {
    loginIn: string;
    signUp: string;
  };
  table: {
    buy: string;
  };
  vipProgram: {
    more: string;
  };
  createAccount: {
    getStarted: string;
  };
}
